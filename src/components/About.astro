<section class="about" id="about" aria-labelledby="about-title">
  <div class="container about__inner">
    <p class="eyebrow about__eyebrow reveal">About</p>

    <h2 id="about-title" class="about__heading reveal">a trust in time.</h2>

    <div class="about__body reveal">
      <p>
        my creation is fueled by a fascination with time. I am curious what
        happens when we learn to trust in the creative process and see where
        it takes us. Everything I create is an expression of the moment.
      </p>
      <p>
        This is a space to share the journey of my creative work and my
        offerings with the people I meet.
      </p>
    </div>

    <p class="about__follow reveal">
      follow my journey online to further explore the intertwining dance
      between time and creation&hellip;
      <a href="https://instagram.com/shalagh" target="_blank" rel="noopener noreferrer" class="about__handle">
        @shalagh
      </a>
    </p>
  </div>
</section>

<style>
  .about {
    padding-block: var(--sp-12) var(--sp-12);
  }

  .about__inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--sp-4);
    max-width: 880px;
    margin: 0 auto;
  }

  .about__eyebrow {
    margin: 0;
    color: var(--color-name);
  }

  .about__heading {
    font-family: var(--font-display);
    font-style: italic;
    font-weight: 500;
    color: var(--color-name);
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    line-height: 1.05;
    letter-spacing: -0.02em;
    margin: 0 0 var(--sp-3);
  }

  .about__body {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--sp-3);
    max-width: 60ch;
  }

  .about__body p {
    margin: 0;
    font-family: var(--font-body);
    font-size: clamp(1.05rem, 1.4vw, 1.35rem);
    line-height: 1.65;
    color: var(--color-ink-soft);
  }

  .about__follow {
    margin: var(--sp-6) 0 0;
    font-family: var(--font-body);
    font-style: italic;
    font-size: clamp(1rem, 1.3vw, 1.2rem);
    line-height: 1.6;
    color: var(--color-ink-soft);
    max-width: 60ch;
  }

  .about__handle {
    display: inline-block;
    margin-left: 6px;
    color: var(--color-name);
    font-style: italic;
    font-weight: 500;
    border-bottom: 1px solid currentColor;
    padding-bottom: 1px;
    transition:
      color var(--duration-fast) var(--ease-out),
      border-color var(--duration-fast) var(--ease-out);
    text-decoration: none;
  }

  .about__handle:hover {
    color: var(--color-brand);
    border-bottom-color: var(--color-brand);
  }

  @media (min-width: 700px) {
    .about__inner {
      gap: var(--sp-5);
    }
  }
</style>
